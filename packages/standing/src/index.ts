/**
 * Standing — what named public raters say about a page's publisher.
 *
 * Static, offline, and always attributed. See `./Standing.ts` for the
 * vocabulary, `./Artifact.ts` for reading the shipped data, and
 * `../tools/build.ts` for how that data is compiled.
 */
export * from "./Artifact.ts"
export * from "./Registrable.ts"
export * from "./Standing.ts"
